import type { ThemeConfig } from "antd";

export const antDesignTheme: ThemeConfig = {
  token: {
    // Seed Token
    colorPrimary: "#0032A0",
    borderRadius: 2,

    // Alias Token
    colorBgContainer: "#f6ffed",
  },
};
